# Generated by dump_packs.mjs / audit.py from the .ts data files and the decks — regenerated on
# demand (audit.py refreshes packs.json when the .ts files are newer), so never committed.
packs.json
audit.json
before.json
*.pkl
__pycache__/
