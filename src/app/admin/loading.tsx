import { RouteLoading } from '@/components/layout/RouteLoading'

// Instant feedback for every navigation in this section — see RouteLoading for why.
export default function Loading() {
  return <RouteLoading />
}
